<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pe.leidy_cs.labcalificado3">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="labcalificado3.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="pe.leidy_cs.labcalificado3.models" />

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ListNoteActivity" />
        <activity android:name=".activities.SignIn" />
        <activity android:name=".activities.SignUp" />
        <activity android:name=".activities.RegisterNote" />
        <activity android:name=".activities.NoteDetailActivity" />
        <activity android:name=".fragments.FirstFragment" />
        <activity android:name=".fragments.SecondFragment" />
        <activity android:name=".fragments.ThirdFragment"></activity>
    </application>

</manifest>